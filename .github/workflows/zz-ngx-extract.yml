name: Streaming NGX native extraction

on:
  push:
    branches: [codex/claude-pass]
    paths: [".github/workflows/zz-ngx-extract.yml"]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/claude-pass
          fetch-depth: 0
      - name: Fix streaming extraction failure path
        run: python3 .github/zz_patch_ngx_extract.py
      - name: Commit source patch
        run: |
          git config user.name "Caustica CI patch"
          git config user.email "actions@users.noreply.github.com"
          git add src/main/java/dev/comfyfluffy/caustica/ngx/NgxRuntime.java
          git commit -m "Fix NGX extraction failure ownership"
          git push origin HEAD:codex/claude-pass
